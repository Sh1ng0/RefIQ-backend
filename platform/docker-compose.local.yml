version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: refiq-app
    ports:
      - "8080:8080"
    # Injects all environment variables from the .env file
    env_file:
      - .env
    depends_on:
      - postgres
      - minio
      - r-engine

  postgres:
    image: postgres:15-alpine
    container_name: refiq-postgres
    ports:
      - "5432:5432"
    environment:
      # Maps .env variables to the official Postgres image requirements
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - postgres_local_data:/var/lib/postgresql/data

  minio:
    image: minio/minio:latest
    container_name: refiq-minio-dev
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      # Maps .env variables to MinIO requirements
      - MINIO_ROOT_USER=${MINIO_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_PASSWORD}
    command: server /data --console-address ":9001"
    volumes:
      - minio_local_data:/data

  r-engine:
    build:
      context: ./src/main/java/com/refiq/engine
      dockerfile: Dockerfile
    container_name: refiq-engine
    ports:
      - "8000:8000"

volumes:
  postgres_local_data:
  minio_local_data: